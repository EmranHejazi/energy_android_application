package app.emran.energy.data;

/**
 * Defines application-wide constants used throughout the app.
 */
public class Constants {
    // Default font size for the app's text elements
    public static final int DEFAULT_FONT_SIZE = 16;
    // Name of the shared preferences file
    public static final String PREFS_NAME = "EnergyAppPrefs";
    // The delay duration of representing for the splash screen (in milliseconds)
    public static final int SPLASH_SCREEN_DELAY = 2000;
}